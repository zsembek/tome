"""Tome — agent-native knowledge OS.

Turns documents into a clean, structured Markdown knowledge base:
pluggable extraction -> LLM structuring with completeness guarantees -> sections/parts ->
hybrid search -> hierarchical Atlas -> REST + MCP + Library UI.
"""

__version__ = "0.1.0"
